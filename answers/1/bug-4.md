# Баг 4 — Поздравление не появляется при значении 10

## Что происходит

Когда счётчик доходит до 10, поздравление не показывается. Оно появляется только при 11 и выше.

## Почему так происходит

В функции `render` проверяется условие `count > 10` (строго больше), тогда как нужно `count === 10`:

```js
// ❌ Баг
function render() {
  counterEl.textContent = count;
  if (count > 10) {
    statusEl.textContent = '🎉 Поздравляем, вы достигли 10!';
  }
}
```

При `count === 10` условие `10 > 10` ложно, поэтому поздравление не срабатывает.

## Как исправить

```js
// ✅ Исправление
if (count === 10) {
  statusEl.textContent = '🎉 Поздравляем, вы достигли 10!';
}
```

## Как лучше

В JavaScript есть два оператора сравнения: `==` (нестрогое) и `===` (строгое). Всегда используй `===` — он сравнивает и значение, и тип, что предотвращает неожиданные совпадения. Например, `10 == "10"` вернёт `true`, а `10 === "10"` — `false`.
